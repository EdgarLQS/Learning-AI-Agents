# 03-工具可靠性

工具可靠性直接影响 Agent 的执行成功率。本文档介绍工具失败的原因和处理机制。

## 工具可靠性问题

```
┌─────────────────────────────────────────────────────────────────┐
│                     工具可靠性问题                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. 工具调用失败                                                │
│     • 网络错误                                                  │
│     • 服务不可用                                                │
│     • 权限问题                                                  │
│                                                                  │
│  2. 参数错误                                                    │
│     • LLM 生成的参数格式错误                                     │
│     • 缺少必需参数                                               │
│     • 参数值超出范围                                             │
│                                                                  │
│  3. 结果解析错误                                                │
│     • 返回格式不符合预期                                         │
│     • 编码问题                                                  │
│     • 超大输出                                                  │
│                                                                  │
│  4. 执行超时                                                   │
│     • 操作耗时过长                                              │
│     • 死循环                                                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 解决方案

### 1. 工具包装器

```python
class ToolWrapper:
    """工具包装器 - 增加可靠性"""

    def __init__(self, tool):
        self.tool = tool

    async def execute(self, *args, **kwargs):
        """带错误处理的执行"""

        try:
            # 参数验证
            self._validate_params(*args, **kwargs)

            # 执行
            result = await self.tool.execute(*args, **kwargs)

            # 结果验证
            self._validate_result(result)

            return result

        except ToolError as e:
            # 可恢复错误，重试
            return await self._retry(*args, **kwargs)

        except Exception as e:
            # 严重错误
            raise ToolExecutionError(f"工具执行失败: {e}")

    def _validate_params(self, *args, **kwargs):
        """参数验证"""
        # 检查必要参数
        # 检查类型
        pass

    def _validate_result(self, result):
        """结果验证"""
        # 检查结果格式
        # 检查是否为错误
        pass
```

### 2. 熔断机制

```python
class ToolCircuitBreaker:
    """工具熔断器"""

    def __init__(self):
        self.failure_counts = {}
        self.circuit_states = {}

    def call(self, tool_name: str, func, *args, **kwargs):
        """熔断调用"""

        if self._is_open(tool_name):
            raise CircuitOpenError(f"工具 {tool_name} 熔断中")

        try:
            result = func(*args, **kwargs)
            self._on_success(tool_name)
            return result

        except Exception as e:
            self._on_failure(tool_name)
            raise
```

## 下一步

- [04-成本与速度](./04-成本与速度.md) - 了解成本优化
- [05-特定语言挑战](./05-特定语言挑战.md) - 了解语言问题